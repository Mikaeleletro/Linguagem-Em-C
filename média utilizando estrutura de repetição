#include <stdio.h> 
int main() { 
 int a, c; 
 float b, d, e; 
 printf("total de alunos "); 
 scanf("%d",&a); 
 b = 0; 
 for(c=1; c<=a; c++) { 
 printf("%da. Nota? ", c); 
 scanf("%f",&d); 
 b = b + d; 
 } 
 e = b / a; 
 printf("media = %.1f\n", e); 
 return 0;
}
